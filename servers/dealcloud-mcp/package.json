{
  "name": "dealcloud-mcp",
  "version": "1.0.0",
  "description": "MCP server for DealCloud REST API - schema, data, and user management",
  "type": "module",
  "bin": {
    "dealcloud-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "tsc --watch",
    "inspect": "npx @modelcontextprotocol/inspector node build/index.js",
    "bundle": "npx esbuild build/index.js --bundle --platform=node --target=node20 --format=cjs --outfile=bundle.cjs",
    "package": "npm run build && npm run bundle && npx @yao-pkg/pkg bundle.cjs --targets node20-macos-arm64,node20-macos-x64,node20-win-x64 --output release/dealcloud-mcp --compress GZip"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
